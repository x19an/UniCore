import NotesModule from "@/components/notes/NotesModule";

export const metadata = {
  title: "Notes - Future Helper",
  description: "Browse and search course notes",
};

export default function NotesPage() {
  return <NotesModule />;
}
